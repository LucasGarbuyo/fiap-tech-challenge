apiVersion: v1
kind: Service
metadata:
  name: cache-service
  labels:
    tier: backend
    layer: cache
spec:
  ports:
  - port: 6379
    protocol: TCP
  selector:
    tier: backend
    layer: cache
  type: ClusterIP
