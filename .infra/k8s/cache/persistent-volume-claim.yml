apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cache-persistent-volume-claim
  labels:
    tier: backend
    layer: cache
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
