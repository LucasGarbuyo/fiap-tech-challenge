apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-cache
  labels:
    tier: backend
    layer: cache
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
